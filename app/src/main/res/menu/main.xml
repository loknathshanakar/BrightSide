<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_brightness"
        android:icon="@drawable/ic_action_brightness"
        android:title="@string/brightness_control"
        app:showAsAction="always" />

    <item
        android:id="@+id/open_fav"
        android:icon="@drawable/ic_action_bookmark_unfilled_action_bar"
        android:title="@string/open_fav"
        app:showAsAction="always" />

    <item
        android:id="@+id/settings"
        android:icon="@drawable/ic_action_settings"
        android:title="@string/open_fav"
        app:showAsAction="always" />
</menu>
